# Node
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Rust / Soroban
target/
**/*.rs.bk
.stellar/
**/test_snapshots/

# Circuit build artifacts (large; regenerated by `npm run circuit:build`)
circuits/build/
*.zkey
*.ptau

# …except the runtime artifacts the app proves with in-browser (must match the
# deployed verification key). Synced into app/public/circuit by the build.
!app/public/circuit/aid_claim.zkey

# Env / secrets
.env
.env.local
*.secret
identities/

# Tooling
.claude/

# OS
.DS_Store
Thumbs.db
.vercel
